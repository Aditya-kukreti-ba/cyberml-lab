export const ALGORITHMS = [
  {
    id: 'logistic_regression',
    name: 'Logistic Regression',
    type: 'classification',
    color: '#B87333',
    icon: 'TrendingUp',
    tagline: 'The classic probabilistic classifier',
    description:
      'Logistic Regression models the probability that an input belongs to a particular class using the logistic (sigmoid) function. Despite its name, it is a classification algorithm that outputs probabilities in [0, 1], making it perfect for binary decisions like "attack vs. normal."',
    howItWorks:
      'The algorithm learns a set of weights (coefficients) for each feature. It computes a weighted sum of features, then squashes the result through the sigmoid function to produce a probability. Predictions are made by applying a threshold (usually 0.5).',
    cybersecurityUse: [
      'Spam / phishing email detection',
      'Intrusion detection (binary: normal vs. attack)',
      'Malware URL classification',
      'Insider threat risk scoring',
    ],
    strengths: [
      'Fast training and inference',
      'Outputs calibrated probabilities',
      'Interpretable coefficients',
      'Works well with linearly separable data',
      'Low computational cost — scales to millions of rows',
    ],
    weaknesses: [
      'Assumes linear decision boundary',
      'Sensitive to outliers and correlated features',
      'Poor on non-linear relationships without feature engineering',
      'Requires feature scaling for best results',
    ],
    whenToUse: 'When you need a fast, interpretable baseline, when the classes are roughly linearly separable, or when you need calibrated probability outputs for risk scoring.',
    whenToAvoid: 'When decision boundaries are highly non-linear, or when features have complex interactions that cannot be expressed linearly.',
    hyperparameters: [
      { name: 'C', description: 'Inverse regularization strength — higher C = less regularization = more complex model' },
      { name: 'max_iter', description: 'Maximum number of solver iterations — increase if model fails to converge' },
      { name: 'solver', description: 'Optimization algorithm — "lbfgs" is a good default for medium datasets' },
    ],
    complexity: 'Low',
    trainingSpeed: 'Very Fast',
    interpretability: 'High',
  },
  {
    id: 'linear_regression',
    name: 'Linear Regression',
    type: 'regression',
    color: '#D6A94A',
    icon: 'TrendingUp',
    tagline: 'The foundational regression technique',
    description:
      'Linear Regression models the relationship between input features and a continuous output target by fitting a straight line (or hyperplane in multi-dimensional space). It minimizes the sum of squared differences between predictions and actual values (Ordinary Least Squares).',
    howItWorks:
      'The model learns a weight for each feature and a bias term. Predictions are a weighted sum of features plus the bias. The optimal weights are found analytically (via matrix operations) or iteratively (gradient descent), making it one of the most computationally efficient ML methods.',
    cybersecurityUse: [
      'Predicting network connection duration',
      'Estimating risk scores or severity ratings',
      'Forecasting data exfiltration volume',
      'Modeling packet loss rates over time',
    ],
    strengths: [
      'Extremely fast to train (closed-form solution)',
      'Highly interpretable — each coefficient has a clear meaning',
      'No hyperparameters to tune by default',
      'Provides a statistical baseline for any regression task',
    ],
    weaknesses: [
      'Assumes a linear relationship — fails on non-linear data',
      'Sensitive to outliers (they heavily influence coefficients)',
      'Assumes features are not strongly correlated (multicollinearity)',
      'Cannot model classification targets directly',
    ],
    whenToUse: 'When target variable has a roughly linear relationship with features, when interpretability is paramount, or as a baseline to beat with more complex models.',
    whenToAvoid: 'When the relationship is non-linear, when there are many outliers, or when the target is a category rather than a continuous value.',
    hyperparameters: [
      { name: 'fit_intercept', description: 'Whether to fit a bias term — almost always True unless data is pre-centered' },
    ],
    complexity: 'Low',
    trainingSpeed: 'Very Fast',
    interpretability: 'Very High',
  },
  {
    id: 'svm',
    name: 'Support Vector Machine',
    type: 'both',
    color: '#7E6BAE',
    icon: 'Filter',
    tagline: 'Maximum-margin classification and regression',
    description:
      'SVMs find the hyperplane that maximizes the margin between classes. Only the "support vectors" — the training points closest to the boundary — determine the final model. The kernel trick allows SVMs to handle non-linear boundaries by mapping data to higher-dimensional spaces implicitly.',
    howItWorks:
      'The SVM solves a constrained quadratic optimization problem to find the maximum-margin hyperplane. With the RBF kernel, it computes pairwise distances in an implicit infinite-dimensional space, allowing it to capture complex non-linear patterns. SVR (the regression variant) fits a "tube" of width ε around the prediction.',
    cybersecurityUse: [
      'Network intrusion detection with complex patterns',
      'Malware classification from behavioral features',
      'Anomaly detection with small labeled datasets',
      'Text classification for phishing content analysis',
    ],
    strengths: [
      'Effective in high-dimensional spaces',
      'Works well with small datasets',
      'Versatile — kernel selection allows non-linear boundaries',
      'Memory efficient — only support vectors are stored',
      'Strong theoretical generalization guarantees',
    ],
    weaknesses: [
      'Slow training on large datasets (O(n²) to O(n³))',
      'Sensitive to feature scaling — always normalize',
      'Kernel and hyperparameter choice requires tuning',
      'Black-box — hard to interpret why a prediction was made',
      'Probability estimates require additional calibration (Platt scaling)',
    ],
    whenToUse: 'When you have small-to-medium datasets with complex, non-linear boundaries. When high accuracy matters more than training speed.',
    whenToAvoid: 'With very large datasets (> 100K rows) where training becomes prohibitively slow. When real-time retraining is required.',
    hyperparameters: [
      { name: 'kernel', description: '"rbf" handles most non-linear cases; "linear" is faster and more interpretable' },
      { name: 'C', description: 'Regularization — lower C = wider margin (more robust); higher C = fits training data more closely' },
      { name: 'gamma', description: 'Kernel coefficient — "scale" is a good default; lower = smoother boundary' },
    ],
    complexity: 'High',
    trainingSpeed: 'Slow',
    interpretability: 'Low',
  },
  {
    id: 'decision_tree',
    name: 'Decision Tree',
    type: 'both',
    color: '#8FAF7A',
    icon: 'GitBranch',
    tagline: 'Rule-based hierarchical prediction',
    description:
      'Decision Trees recursively partition the feature space into regions using a series of if-else rules. Each internal node tests a feature condition, each branch represents an outcome, and each leaf node gives a prediction. The result is a fully interpretable model that mimics human decision-making.',
    howItWorks:
      'The tree is built by choosing the feature and threshold at each node that best separates the classes (using Gini impurity or entropy for classification, or MSE for regression). This greedy splitting continues until a stopping criterion is met (max depth, min samples, etc.).',
    cybersecurityUse: [
      'Firewall rule generation from labeled traffic',
      'Threat severity classification with audit trails',
      'Intrusion detection with explainable decisions',
      'Malware family classification from API call sequences',
    ],
    strengths: [
      'Fully interpretable — can be visualized as rules',
      'No feature scaling required',
      'Handles both numerical and categorical features naturally',
      'Captures non-linear relationships',
      'Fast training and inference',
    ],
    weaknesses: [
      'Prone to overfitting — deep trees memorize training data',
      'Unstable — small data changes can produce very different trees',
      'Biased toward features with more levels',
      'Not competitive with ensemble methods in accuracy',
    ],
    whenToUse: 'When model explainability is required (regulatory compliance, security audits), or as a baseline for ensemble methods (Random Forests).',
    whenToAvoid: 'When highest accuracy is the primary goal — prefer ensembles. When the dataset is very small and overfitting is a major concern.',
    hyperparameters: [
      { name: 'max_depth', description: 'Limits tree depth to prevent overfitting — try 3–10 for most tasks' },
      { name: 'min_samples_split', description: 'Minimum samples required to split a node — higher values prevent overfitting' },
      { name: 'criterion', description: '"gini" is fast; "entropy" (information gain) sometimes gives better trees' },
    ],
    complexity: 'Medium',
    trainingSpeed: 'Fast',
    interpretability: 'Very High',
  },
  {
    id: 'naive_bayes',
    name: 'Naive Bayes',
    type: 'classification',
    color: '#B84A4A',
    icon: 'Brain',
    tagline: 'Probabilistic classification via Bayes theorem',
    description:
      'Naive Bayes applies Bayes theorem with a "naive" assumption that all features are conditionally independent given the class. Despite this strong assumption rarely holding in practice, it performs surprisingly well on many real-world classification tasks, especially text and network data.',
    howItWorks:
      'For each class, the model estimates P(feature | class) from training data. At prediction time, it computes P(class | features) ∝ P(class) × ∏ P(feature_i | class) using the independence assumption, then picks the class with the highest posterior probability.',
    cybersecurityUse: [
      'Spam and phishing detection (originally Bayesian spam filters)',
      'Log anomaly classification',
      'Network protocol classification',
      'Malicious domain name detection from lexical features',
    ],
    strengths: [
      'Extremely fast training and prediction',
      'Works well with small datasets',
      'Naturally handles multi-class problems',
      'Robust to irrelevant features',
      'Outputs calibrated probability estimates',
    ],
    weaknesses: [
      'Independence assumption is rarely true in practice',
      'Cannot model feature correlations or interactions',
      'Zero-frequency problem for unseen feature values (requires smoothing)',
      'Typically outperformed by more complex models on structured data',
    ],
    whenToUse: 'As a fast, robust baseline especially for text classification, log analysis, and protocol detection. When training data is scarce.',
    whenToAvoid: 'When features are strongly correlated, or when maximum accuracy is required on structured tabular data.',
    hyperparameters: [
      { name: 'var_smoothing', description: 'Adds a small variance to all features for numerical stability (Laplace smoothing)' },
    ],
    complexity: 'Low',
    trainingSpeed: 'Very Fast',
    interpretability: 'Medium',
  },
];

export const ALGORITHM_MAP = Object.fromEntries(ALGORITHMS.map((a) => [a.id, a]));
